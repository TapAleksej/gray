/var/log/nginx/*.log {
    weekly
    rotate {{ logrotate.rotate }}
    size {{ logrotate.size }}
    compress
    delaycompress
    missingok
    notifempty
    sharedscripts
    postrotate
        systemctl reload nginx > /dev/null 2>&1 || true
    endscript
}
